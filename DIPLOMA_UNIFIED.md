# ПОЯСНИТЕЛЬНАЯ ЗАПИСКА

**Тема:** Проектирование и разработка информационной системы `DeliveryCRM`

## СОДЕРЖАНИЕ

ВВЕДЕНИЕ  
1 АНАЛИТИЧЕСКАЯ ЧАСТЬ  
1.1 Описание предметной области  
1.2 Обзор аналогов  
2 ПРОЕКТИРОВАНИЕ ПРОГРАММНОГО ПРОДУКТА  
2.1 Определение требований к системе  
2.2 Архитектура информационной системы  
2.3 Проектирование базы данных  
2.4 Проектирование пользовательского интерфейса  
2.5 Диаграмма вариантов использования (Диаграмма Use-case)  
3 РЕАЛИЗАЦИЯ ПРОЕКТА  
3.1 Обоснование выбора языка и средства разработки  
3.2 Создание проекта  
4 ТЕСТИРОВАНИЕ ПРОГРАММНОГО ПРОДУКТА  
4.1 Основы тестирования программного продукта  
4.2 Тест-кейс тестирование  
5 РУКОВОДСТВО ПОЛЬЗОВАТЕЛЯ  
5.1 Руководство пользователя для клиента  
5.2 Руководство пользователя для сотрудников кухни  
5.3 Руководство пользователя для курьера  
5.4 Руководство пользователя для администратора  
ЗАКЛЮЧЕНИЕ  
СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ  
ПРИЛОЖЕНИЕ А  
ПРИЛОЖЕНИЕ Б  
ПРИЛОЖЕНИЕ В

## ВВЕДЕНИЕ

В условиях цифровой трансформации логистической отрасли предприятиям доставки требуется единая информационная система, объединяющая клиентский сервис, управление заказами, контроль исполнения доставки и управленческую аналитику.

Объект исследования - деятельность предприятия в сфере доставки.  
Предмет исследования - процессы автоматизации управления заказами, клиентами, курьерами и аналитикой.

**Цель работы:** разработка информационной системы `DeliveryCRM`, обеспечивающей централизованное управление процессами службы доставки и повышение качества обслуживания клиентов.

Для достижения поставленной цели необходимо решить следующие задачи:
1. Провести анализ предметной области и существующих аналогов.  
2. Сформировать требования к системе и спроектировать архитектуру решения.  
3. Спроектировать структуру базы данных и пользовательские интерфейсы.  
4. Реализовать серверную и клиентскую части системы с разграничением прав доступа.  
5. Реализовать аналитический контур и формы отчетности.  
6. Провести тестирование ключевых сценариев работы системы.  
7. Подготовить руководство пользователя и приложения с SQL и программным кодом.

## 1 АНАЛИТИЧЕСКАЯ ЧАСТЬ

### 1.1 Описание предметной области

Предметной областью разрабатываемого программного продукта является управление процессами городской и межрайонной доставки в формате CRM-системы для логистической компании. В рамках данной области одновременно решаются задачи клиентского сервиса, операционного управления курьерской сетью, контроля выполнения заказов, внутренней коммуникации сотрудников и управленческой аналитики.

Классическая проблема предприятий доставки заключается в том, что ключевые процессы часто разнесены по разным инструментам: прием заказов ведется в одном приложении, контроль курьеров - в другом, общение сотрудников - в мессенджерах, а отчеты и аналитика - в таблицах. Это приводит к потере целостности данных, задержкам в принятии решений, сложностям контроля качества сервиса и росту операционных затрат.

Разрабатываемая система `DeliveryCRM` решает указанную проблему за счет единой информационной платформы, включающей API и web-клиент на Blazor, а также мобильный контур на Flutter. Система ориентирована на многоуровневую работу пользователей с различными ролями и правами:
- клиент;
- менеджер;
- логист;
- администратор;
- курьер.

Основные сущности предметной области включают:
- компанию, реализующую мультиарендную модель;
- пользователя и роль;
- профиль клиента;
- профиль курьера;
- заказ доставки;
- адреса точек забора и доставки;
- транспортное средство;
- лид и стадии воронки продаж;
- уведомления;
- сообщения чата;
- аудит действий.

Таким образом, предметная область объединяет CRM-процессы и TMS-процессы. Это определяет требования к системе: высокая связанность модулей, оперативный обмен данными в реальном времени и строгая ролевая модель доступа.

Предметная область характеризуется высокой связностью бизнес-процессов и необходимостью оперативного обмена данными. Для обеспечения управляемости требуется единая цифровая платформа с общей моделью данных и ролевым доступом.

### 1.2 Обзор аналогов

Анализ аналогов позволяет выявить сильные и слабые стороны существующих решений, определить функциональные разрывы и обосновать требования к разрабатываемому продукту. Для предметной области службы доставки рассмотрены три типа решений: универсальная CRM-платформа, специализированная логистическая платформа и разрозненный набор инструментов, часто используемый малыми и средними компаниями на этапе роста.

Сервис `Bitrix24` (https://www.bitrix24.ru) (см. рисунок 1.1) относится к универсальным CRM-системам и широко применяется для управления клиентской базой, сделками, задачами и воронкой продаж [3].

**Рисунок 1.1 - Интерфейс сервиса Bitrix24**  
`[МЕСТО ДЛЯ РИСУНКА 1.1]`

`Bitrix24` обеспечивает зрелый CRM-контур, но для логистического направления требует глубокой адаптации.

Плюсы:
1. Развитые инструменты управления клиентской базой и коммуникациями.
2. Встроенные отчеты по воронке продаж и активности сотрудников.
3. Наличие автоматизации бизнес-процессов и шаблонов сценариев.
4. Развитая экосистема интеграций и расширений.

Минусы для службы доставки:
1. Недостаточная глубина нативного логистического контура.
2. Ограниченная поддержка диспетчеризации и маршрутизации в базовой конфигурации.
3. Необходимость дополнительной настройки под роли логиста и курьера.
4. Рост стоимости сопровождения при сложной кастомизации.

Сервис `Onfleet` (https://onfleet.com) (см. рисунок 1.2) представляет класс специализированных платформ last-mile доставки и ориентирован на управление маршрутами, статусами и задачами курьеров [5].

**Рисунок 1.2 - Интерфейс сервиса Onfleet**  
`[МЕСТО ДЛЯ РИСУНКА 1.2]`

`Onfleet` демонстрирует высокую зрелость в операционной логистике, однако закрывает CRM-направление частично.

Плюсы:
1. Развитая диспетчеризация и контроль исполнения доставки.
2. Инструменты мониторинга статусов и геопозиции курьеров.
3. Поддержка оптимизации маршрутов и загрузки ресурсов.
4. Фокус на снижении времени доставки и операционных задержек.

Минусы для комплексного CRM-сценария:
1. Ограниченные возможности работы с лидами и клиентской историей.
2. Недостаточная глубина функций сегментации и удержания клиентов.
3. Потребность в интеграции с внешними CRM-модулями.
4. Сложность адаптации под полный цикл клиентского сопровождения.

В качестве третьего аналога рассмотрен типовой набор `Google Sheets + Telegram` (см. рисунок 1.3), который используется многими компаниями как временная операционная схема.

**Рисунок 1.3 - Пример разрозненного контура (Google Sheets + Telegram)**  
`[МЕСТО ДЛЯ РИСУНКА 1.3]`

Подход обеспечивает быстрый запуск процессов, но теряет эффективность при росте заказов и ролей.

Плюсы:
1. Минимальные затраты на старт и внедрение.
2. Низкий порог входа для персонала.
3. Быстрое изменение рабочих шаблонов на раннем этапе.

Минусы:
1. Отсутствие единой модели данных и централизованной бизнес-логики.
2. Высокая зависимость от ручных операций и человеческого фактора.
3. Слабая масштабируемость при росте потока заказов.
4. Затрудненный аудит действий и контроль SLA.

Сравнительный анализ рассмотренных решений приведен в таблице 1.1 (см. таблицу 1.1).

**Таблица 1.1 - Сравнительный анализ аналогов**  
| Критерий | Bitrix24 | Onfleet | Google Sheets + Telegram | DeliveryCRM |
|---|---|---|---|---|
| CRM-контур (клиенты, карточки, воронка) | + | - | - | + |
| Диспетчеризация и управление доставкой | - | + | - | + |
| Realtime-коммуникации в едином контуре | - | +/- | - | + |
| Ролевая модель доступа (клиент/менеджер/логист/курьер/админ) | +/- | +/- | - | + |
| Tenant-изоляция данных | - | - | - | + |
| Аудит действий пользователей | +/- | +/- | - | + |
| Централизованная аналитика | + | +/- | - | + |
| Глубокая интеграция под процессы конкретной компании | - | - | - | + |
| Стоимость и сложность внедрения для малого/среднего бизнеса | Средняя | Высокая | Низкая на старте, высокая при росте | Сбалансированная |

Проведенный обзор показывает, что универсальные CRM-системы и логистические платформы покрывают разные части задачи, но не формируют в полном объеме единый контур под процессы конкретной компании доставки без значительной доработки. Разработка `DeliveryCRM` обоснована необходимостью объединить CRM-функции, диспетчеризацию, ролевой контроль, realtime-коммуникации и аналитический контур в одной системе с управляемой стоимостью внедрения.

## 2 ПРОЕКТИРОВАНИЕ ПРОГРАММНОГО ПРОДУКТА

### 2.1 Определение требований к системе

Требования к системе сформированы на основе анализа процессов городской и межрайонной доставки, а также структуры ролей в компании. При определении требований учитывались операционная устойчивость, безопасность данных, прозрачность статусов и возможность масштабирования при росте нагрузки.

Требования к архитектуре информационной системы включают использование клиент-серверной модели, единый API-контур и централизованную бизнес-логику. Доступ к функционалу должен обеспечиваться через многоуровневую аутентификацию и авторизацию для пользователей различных ролей.

Требования к безопасности предусматривают защиту персональных данных и служебной информации, разграничение прав доступа, использование защищенных механизмов хранения учетных данных и регулярное резервное копирование. Для учетных записей применяется хеширование паролей, а межсервисный обмен выполняется через защищенные каналы.

Требования к отображению интерфейсов предполагают корректную работу web-интерфейсов на стационарных устройствах и адаптацию мобильных интерфейсов под экраны различного размера. Интерфейсы должны сохранять читаемость, логическую структуру и доступность ключевых операций при изменении разрешения и ориентации экрана.

Требования к надежности и эксплуатации включают контроль целостности данных при параллельной работе пользователей, предсказуемое поведение системы при временной недоступности внешних сервисов и наличие механизмов восстановления после отказов. Дополнительно учитывается необходимость журналирования критичных операций, чтобы обеспечить возможность последующего аудита и анализа инцидентов.

Требования к производительности определяют допустимое время отклика для ключевых сценариев: авторизация, создание заказа, изменение статуса, назначение курьера и загрузка аналитических представлений. Для поддержания производительности при росте нагрузки предусматриваются индексация ключевых таблиц, оптимизация запросов и масштабирование серверного контура без изменения пользовательских сценариев.

К функциональным требованиям относятся:
- регистрация и авторизация пользователей по ролям;
- управление заказами на всех этапах жизненного цикла;
- распределение заказов между курьерами и контроль исполнения;
- работа с клиентским контуром, уведомлениями и чатами;
- формирование отчетов и аналитических срезов по операционной деятельности;
- управление сотрудниками, ролями и правами доступа;
- аудит действий в критичных сценариях изменения данных.

К нефункциональным требованиям относятся:
- обеспечение безопасности доступа к API и данным;
- tenant-изоляция данных между компаниями;
- устойчивость системы при одновременной работе нескольких ролей;
- масштабируемость архитектуры при увеличении объема заказов;
- прозрачность аудита пользовательских действий;
- контролируемое время отклика для критичных операций;
- возможность расширения функционала без реорганизации базовой архитектуры.

**Таблица 2.1 - Матрица требований к DeliveryCRM**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.1]`

Сформированный набор требований покрывает как операционный контур, так и технические ограничения эксплуатации (см. таблицу 2.1). Полученные требования используются в качестве основы для проектирования архитектуры, базы данных и интерфейсов системы и напрямую влияют на структуру последующих проектных решений. Полнота требований позволяет согласовать ожидания пользователей, проектные ограничения и критерии приемки на этапах реализации и тестирования.

### 2.2 Архитектура информационной системы

Архитектура решения построена по клиент-серверной модели и включает разделение системы на представление, бизнес-логику и хранение данных. Такая декомпозиция позволяет обеспечить управляемость проекта и независимое развитие ключевых подсистем.

Технологический контур архитектуры:
- серверная часть: `ASP.NET Core Web API`;
- web-клиент: `Blazor Server`;
- мобильный контур: клиентское приложение на `Flutter`;
- база данных: `PostgreSQL` с использованием `Entity Framework Core`;
- контур realtime-взаимодействия: `SignalR`.

На серверном уровне реализуются аутентификация, авторизация, правила бизнес-логики, tenant-изоляция и аудит действий. На клиентском уровне реализуются ролевые сценарии работы пользователей, включая интерфейсы менеджера, логиста, курьера, администратора и клиента.

Все клиентские приложения взаимодействуют с сервером через единый API-контур, что обеспечивает согласованность бизнес-правил и исключает дублирование критичной логики в интерфейсных слоях. Такой подход снижает риск рассогласования данных и повышает предсказуемость поведения системы при изменении функционала.

Архитектура предусматривает расширяемость по функциональным доменам: заказы, пользователи, логистика, коммуникации, биллинг и отчетность. Разделение по доменам упрощает сопровождение кода, локализацию изменений и внедрение новых модулей без риска регрессии в несвязанных частях системы.

Для обмена данными между слоями применяются типизированные DTO-модели и единые правила валидации. Такой подход снижает вероятность ошибок интеграции, упрощает контроль контрактов и обеспечивает предсказуемое поведение API для всех клиентских приложений.

**Рисунок 2.1 - Архитектура информационной системы DeliveryCRM**  
`[МЕСТО ДЛЯ РИСУНКА 2.1]`

Архитектурная схема информационной системы приведена на рисунке 2.1 (см. рисунок 2.1). Выбранная архитектура обеспечивает разделение ответственности между слоями и масштабируемость при росте нагрузки. Централизация бизнес-правил на уровне API повышает согласованность обработки данных и надежность работы системы в многопользовательском режиме. За счет унификации контрактов и ролевой модели архитектура поддерживает дальнейшее масштабирование и поэтапное развитие продукта.

### 2.3 Проектирование базы данных

В рамках разработки DeliveryCRM в качестве СУБД используется `PostgreSQL`. Выбор обусловлен сочетанием производительности и надежности, полноценной поддержкой транзакций, а также возможностью эффективно выполнять как операционные, так и аналитические SQL-запросы. Дополнительными факторами стали свободная лицензия, развитый инструментарий администрирования (`pgAdmin`) и корректная интеграция с `Entity Framework Core`, применяемым на уровне API.

Структура базы данных проектировалась на основе принципов нормализации. Это позволило минимизировать дублирование данных, сохранить согласованность связей между сущностями и упростить развитие схемы при добавлении новых модулей. Для основных таблиц зафиксированы первичные ключи, внешние ключи и ограничения целостности, отражающие бизнес-правила логистической системы.

В результате сформирован словарь данных, включающий 17 основных таблиц. В таблицах 2.2-2.18 представлено описание структуры каждой таблицы базы данных. Для таблиц словаря данных применяется шрифт 12 и межстрочный интервал 1.

Для повышения читаемости модели и удобства сопровождения в проекте используется логическое разбиение таблиц по схемам PostgreSQL: `пользователи_и_доступ`, `заказы`, `логистика_и_смены`, `автопарк`, `коммуникации`, `биллинг_и_подписки`, `аналитика_и_crm`. Такое разбиение не нарушает ссылочную целостность и позволяет отражать структуру БД в отчете по тематическим блокам.

**Таблица 2.2 - Словарь данных таблицы Companies**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.2]`

**Таблица 2.3 - Словарь данных таблицы Roles**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.3]`

**Таблица 2.4 - Словарь данных таблицы Users**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.4]`

**Таблица 2.5 - Словарь данных таблицы Logins**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.5]`

**Таблица 2.6 - Словарь данных таблицы ClientProfiles**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.6]`

**Таблица 2.7 - Словарь данных таблицы ManagerProfiles**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.7]`

**Таблица 2.8 - Словарь данных таблицы CourierProfiles**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.8]`

**Таблица 2.9 - Словарь данных таблицы Addresses**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.9]`

**Таблица 2.10 - Словарь данных таблицы Orders**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.10]`

**Таблица 2.11 - Словарь данных таблицы OrderStatuses**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.11]`

**Таблица 2.12 - Словарь данных таблицы OrderTimelineEvents**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.12]`

**Таблица 2.13 - Словарь данных таблицы ChatRooms**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.13]`

**Таблица 2.14 - Словарь данных таблицы ChatParticipants**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.14]`

**Таблица 2.15 - Словарь данных таблицы ChatMessages**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.15]`

**Таблица 2.16 - Словарь данных таблицы Notifications**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.16]`

**Таблица 2.17 - Словарь данных таблицы BillingInvoices**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.17]`

**Таблица 2.18 - Словарь данных таблицы PaymentTransactions**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 2.18]`

После словаря данных приводятся текстовые команды `CREATE` и `INSERT` для создания таблиц и заполнения начальными данными. В проекте применяется тематическое **структурирование таблиц по схемам**, что повышает управляемость и читаемость модели данных.

```sql
CREATE DATABASE "DeliveryCRM";
\c "DeliveryCRM";

CREATE SCHEMA IF NOT EXISTS "пользователи_и_доступ";
CREATE SCHEMA IF NOT EXISTS "заказы";
CREATE SCHEMA IF NOT EXISTS "логистика_и_смены";
CREATE SCHEMA IF NOT EXISTS "коммуникации";
CREATE SCHEMA IF NOT EXISTS "биллинг_и_подписки";

CREATE TABLE "пользователи_и_доступ"."Companies" (
    "ID_Company" SERIAL PRIMARY KEY,
    "Name" VARCHAR(200) NOT NULL,
    "Subdomain" VARCHAR(100),
    "Created_at" TIMESTAMP NOT NULL DEFAULT NOW(),
    "Is_Active" BOOLEAN NOT NULL DEFAULT TRUE
);

CREATE TABLE "пользователи_и_доступ"."Roles" (
    "ID_Role" SERIAL PRIMARY KEY,
    "Name" VARCHAR(100) NOT NULL
);

CREATE TABLE "пользователи_и_доступ"."Users" (
    "ID_User" SERIAL PRIMARY KEY,
    "FName" VARCHAR(100) NOT NULL,
    "Name" VARCHAR(100) NOT NULL,
    "Patronumic" VARCHAR(100),
    "Created_at" TIMESTAMP NOT NULL DEFAULT NOW(),
    "Is_Active" BOOLEAN NOT NULL DEFAULT TRUE,
    "Theme" VARCHAR(50) NOT NULL DEFAULT 'light',
    "Avatar" VARCHAR(500) NOT NULL DEFAULT '/avatars/default.png',
    "Company_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Companies"("ID_Company"),
    "Role_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Roles"("ID_Role")
);

CREATE TABLE "пользователи_и_доступ"."Logins" (
    "ID_Login" SERIAL PRIMARY KEY,
    "Email" VARCHAR(256) NOT NULL UNIQUE,
    "Password" VARCHAR(256) NOT NULL,
    "ID_User" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Users"("ID_User")
);

CREATE TABLE "заказы"."OrderStatuses" (
    "ID_OrderStatus" SERIAL PRIMARY KEY,
    "Name" VARCHAR(100) NOT NULL,
    "Description" VARCHAR(500)
);

CREATE TABLE "заказы"."OrderTypes" (
    "ID_OrderType" SERIAL PRIMARY KEY,
    "Name" VARCHAR(100) NOT NULL
);

CREATE TABLE "заказы"."PackageTypes" (
    "ID_PackageType" SERIAL PRIMARY KEY,
    "Name" VARCHAR(100) NOT NULL
);

CREATE TABLE "заказы"."PaymentMethods" (
    "ID_PaymentMethod" SERIAL PRIMARY KEY,
    "Name" VARCHAR(100) NOT NULL
);

CREATE TABLE "пользователи_и_доступ"."ClientProfiles" (
    "ID_ClientProfile" SERIAL PRIMARY KEY,
    "Company_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Companies"("ID_Company"),
    "User_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Users"("ID_User"),
    "Preferred_payment_method_id" INTEGER NOT NULL REFERENCES "заказы"."PaymentMethods"("ID_PaymentMethod"),
    "Default_address" VARCHAR(500) NOT NULL DEFAULT '',
    "Rating" NUMERIC(5,2) NOT NULL DEFAULT 0
);

CREATE TABLE "пользователи_и_доступ"."ManagerProfiles" (
    "ID_ManagerProfile" SERIAL PRIMARY KEY,
    "Company_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Companies"("ID_Company"),
    "User_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Users"("ID_User"),
    "Position" VARCHAR(100),
    "Department" VARCHAR(50),
    "HireDate" TIMESTAMP NOT NULL DEFAULT NOW(),
    "Is_Active" BOOLEAN NOT NULL DEFAULT TRUE
);

CREATE TABLE "пользователи_и_доступ"."CourierProfiles" (
    "ID_CourierProfile" SERIAL PRIMARY KEY,
    "Company_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Companies"("ID_Company"),
    "User_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Users"("ID_User"),
    "DriverLicense" VARCHAR(200),
    "Passport_data" VARCHAR(200),
    "Rating" NUMERIC(5,2) NOT NULL DEFAULT 0,
    "Total_deliveries" INTEGER NOT NULL DEFAULT 0,
    "Is_online" BOOLEAN NOT NULL DEFAULT FALSE,
    "Current_lat" NUMERIC(12,7) NOT NULL DEFAULT 0,
    "Current_lon" NUMERIC(12,7) NOT NULL DEFAULT 0,
    "LastActivity_at" TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE "логистика_и_смены"."Addresses" (
    "ID_Address" SERIAL PRIMARY KEY,
    "Street" VARCHAR(200) NOT NULL,
    "House" VARCHAR(50) NOT NULL,
    "Flat" VARCHAR(50),
    "City" VARCHAR(100),
    "Region" VARCHAR(100),
    "PostalCode" VARCHAR(20),
    "Comment" VARCHAR(500),
    "Latitude" NUMERIC(12,7),
    "Longitude" NUMERIC(12,7),
    "Company_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Companies"("ID_Company"),
    "User_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Users"("ID_User")
);

CREATE TABLE "заказы"."Orders" (
    "ID_Order" SERIAL PRIMARY KEY,
    "Name" VARCHAR(200) NOT NULL,
    "Description" TEXT NOT NULL,
    "Order_Number" INTEGER NOT NULL,
    "Company_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Companies"("ID_Company"),
    "Client_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."ClientProfiles"("ID_ClientProfile"),
    "OrderType_id" INTEGER NOT NULL REFERENCES "заказы"."OrderTypes"("ID_OrderType"),
    "Status_id" INTEGER NOT NULL REFERENCES "заказы"."OrderStatuses"("ID_OrderStatus"),
    "Courier_id" INTEGER REFERENCES "пользователи_и_доступ"."CourierProfiles"("ID_CourierProfile"),
    "PackageType_id" INTEGER NOT NULL REFERENCES "заказы"."PackageTypes"("ID_PackageType"),
    "Weight" NUMERIC(10,2) NOT NULL DEFAULT 0,
    "Height" NUMERIC(10,2) NOT NULL DEFAULT 0,
    "Length" NUMERIC(10,2) NOT NULL DEFAULT 0,
    "Width" NUMERIC(10,2) NOT NULL DEFAULT 0,
    "Estimated_cost" NUMERIC(12,2) NOT NULL DEFAULT 0,
    "Final_cost" NUMERIC(12,2) NOT NULL DEFAULT 0,
    "Created_at" TIMESTAMP NOT NULL DEFAULT NOW(),
    "Delivered_at" TIMESTAMP NULL,
    "PaymentMethod_id" INTEGER NOT NULL REFERENCES "заказы"."PaymentMethods"("ID_PaymentMethod"),
    "Is_paid" BOOLEAN NOT NULL DEFAULT FALSE,
    "PickupAddress_id" INTEGER NOT NULL REFERENCES "логистика_и_смены"."Addresses"("ID_Address"),
    "DeliveryAddress_id" INTEGER NOT NULL REFERENCES "логистика_и_смены"."Addresses"("ID_Address")
);

CREATE TABLE "заказы"."OrderTimelineEvents" (
    "ID_OrderTimelineEvent" SERIAL PRIMARY KEY,
    "Order_id" INTEGER NOT NULL REFERENCES "заказы"."Orders"("ID_Order"),
    "EventType" VARCHAR(50) NOT NULL,
    "Title" VARCHAR(100),
    "Message" VARCHAR(1500),
    "OldStatus_id" INTEGER,
    "NewStatus_id" INTEGER,
    "OldCourier_id" INTEGER,
    "NewCourier_id" INTEGER,
    "ActorUser_id" INTEGER,
    "Created_at" TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE "коммуникации"."ChatRoomTypes" (
    "ID_ChatRoomType" SERIAL PRIMARY KEY,
    "Name" VARCHAR(50) NOT NULL
);

CREATE TABLE "коммуникации"."ChatRooms" (
    "ID_ChatRoom" SERIAL PRIMARY KEY,
    "Company_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Companies"("ID_Company"),
    "Name" VARCHAR(200),
    "ChatRoomType_id" INTEGER NOT NULL REFERENCES "коммуникации"."ChatRoomTypes"("ID_ChatRoomType"),
    "Order_id" INTEGER REFERENCES "заказы"."Orders"("ID_Order"),
    "Created_at" TIMESTAMP NOT NULL DEFAULT NOW(),
    "LastMessage_at" TIMESTAMP
);

CREATE TABLE "коммуникации"."ChatParticipants" (
    "ID_ChatParticipant" SERIAL PRIMARY KEY,
    "ChatRoom_id" INTEGER NOT NULL REFERENCES "коммуникации"."ChatRooms"("ID_ChatRoom"),
    "User_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Users"("ID_User"),
    "Joined_at" TIMESTAMP NOT NULL DEFAULT NOW(),
    "Left_at" TIMESTAMP,
    "Is_active" BOOLEAN NOT NULL DEFAULT TRUE,
    "LastRead_at" TIMESTAMP
);

CREATE TABLE "коммуникации"."ChatMessages" (
    "ID_ChatMessage" SERIAL PRIMARY KEY,
    "ChatRoom_id" INTEGER NOT NULL REFERENCES "коммуникации"."ChatRooms"("ID_ChatRoom"),
    "Sender_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Users"("ID_User"),
    "MessageText" VARCHAR(5000) NOT NULL,
    "AttachmentUrl" VARCHAR(500),
    "Sent_at" TIMESTAMP NOT NULL DEFAULT NOW(),
    "Edited_at" TIMESTAMP,
    "Is_deleted" BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE TABLE "коммуникации"."NotificationTypes" (
    "ID_NotificationType" SERIAL PRIMARY KEY,
    "Name" VARCHAR(100) NOT NULL
);

CREATE TABLE "коммуникации"."Notifications" (
    "ID_Notification" SERIAL PRIMARY KEY,
    "Company_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Companies"("ID_Company"),
    "User_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Users"("ID_User"),
    "Type_id" INTEGER NOT NULL REFERENCES "коммуникации"."NotificationTypes"("ID_NotificationType"),
    "Title" VARCHAR(200) NOT NULL,
    "Message" TEXT NOT NULL,
    "Order_id" INTEGER REFERENCES "заказы"."Orders"("ID_Order"),
    "Is_read" BOOLEAN NOT NULL DEFAULT FALSE,
    "Priority" SMALLINT NOT NULL DEFAULT 0,
    "Is_critical" BOOLEAN NOT NULL DEFAULT FALSE,
    "Requires_ack" BOOLEAN NOT NULL DEFAULT FALSE,
    "Acknowledged_at" TIMESTAMP,
    "Sent_at" DATE NOT NULL DEFAULT CURRENT_DATE
);

CREATE TABLE "биллинг_и_подписки"."SubscriptionPlans" (
    "ID_SubscriptionPlan" SERIAL PRIMARY KEY,
    "Name" VARCHAR(100) NOT NULL
);

CREATE TABLE "биллинг_и_подписки"."BillingInvoices" (
    "ID_BillingInvoice" SERIAL PRIMARY KEY,
    "Company_id" INTEGER NOT NULL REFERENCES "пользователи_и_доступ"."Companies"("ID_Company"),
    "SubscriptionPlan_id" INTEGER NOT NULL REFERENCES "биллинг_и_подписки"."SubscriptionPlans"("ID_SubscriptionPlan"),
    "Number" VARCHAR(50) NOT NULL,
    "Amount" NUMERIC(12,2) NOT NULL,
    "Currency" VARCHAR(10) NOT NULL DEFAULT 'RUB',
    "Status" VARCHAR(20) NOT NULL DEFAULT 'pending',
    "Issued_at" TIMESTAMP NOT NULL DEFAULT NOW(),
    "Due_at" TIMESTAMP NOT NULL,
    "Paid_at" TIMESTAMP,
    "PeriodMonths" INTEGER NOT NULL DEFAULT 1
);

CREATE TABLE "биллинг_и_подписки"."PaymentTransactions" (
    "ID_PaymentTransaction" SERIAL PRIMARY KEY,
    "BillingInvoice_id" INTEGER NOT NULL REFERENCES "биллинг_и_подписки"."BillingInvoices"("ID_BillingInvoice"),
    "Provider" VARCHAR(50) NOT NULL DEFAULT 'MockPay',
    "ProviderPaymentId" VARCHAR(100) NOT NULL,
    "Status" VARCHAR(20) NOT NULL DEFAULT 'initiated',
    "Amount" NUMERIC(12,2) NOT NULL,
    "Created_at" TIMESTAMP NOT NULL DEFAULT NOW(),
    "Succeeded_at" TIMESTAMP,
    "FailureReason" TEXT
);

INSERT INTO "пользователи_и_доступ"."Roles" ("Name") VALUES
('Администратор'), ('Менеджер'), ('Логист'), ('Курьер'), ('Клиент');

INSERT INTO "заказы"."OrderStatuses" ("Name", "Description") VALUES
('Создан', 'Заказ создан клиентом'),
('Принят', 'Заказ принят менеджером'),
('В обработке', 'Заказ передан в операционный контур'),
('Назначен курьеру', 'Курьер назначен на доставку'),
('В доставке', 'Курьер выполняет доставку'),
('Доставлен', 'Заказ успешно завершен'),
('Отменен', 'Заказ отменен');
```

В приложении А содержатся данные таблиц.

```sql
SELECT "ID_Order", "Order_Number", "Created_at", "Status_id"
FROM "заказы"."Orders"
ORDER BY "Created_at" DESC;
```

```sql
SELECT "OrderType_id", COUNT(*) AS "OrdersCount", SUM("Final_cost") AS "Revenue"
FROM "заказы"."Orders"
GROUP BY "OrderType_id"
ORDER BY "OrdersCount" DESC;
```

Текст SQL-скриптов для полного набора таблиц и заполнения справочных данных приводится в приложении А (см. приложение А). Примеры программной реализации запросов и листинги ключевых модулей приводятся в приложении Б (см. приложение Б).

Разработанная структура базы данных охватывает ключевые сущности предметной области и поддерживает масштабирование системы при росте числа пользователей, заказов и событий взаимодействия. Использование внешних ключей обеспечивает ссылочную целостность, а индексация часто запрашиваемых полей повышает производительность аналитических и операционных запросов. Порядок представления материала в подразделе соблюден в полном объеме: словарь данных, затем `CREATE`/`INSERT`, далее ссылка на приложение А и только после этого `SELECT`-запросы текстом.

### 2.4 Проектирование пользовательского интерфейса

После проектирования пользовательского интерфейса были определены доступные экраны и операции для каждой роли в DeliveryCRM в соответствии с уровнем доступа. Для каждой категории пользователей формализованы сценарии работы и ограничения, что позволило исключить пересечение ответственности между участниками операционного процесса.

Ролевая модель интерфейса обеспечивает структурированность архитектуры и возможность гибкой адаптации функционала под задачи конкретной группы пользователей. В системе выделены пять основных категорий: администратор, менеджер, логист, курьер и клиент.

**Таблица 2.1 - Роли пользователей и доступный функционал**

| Функциональная область | Администратор | Менеджер | Логист | Курьер | Клиент |
|---|---|---|---|---|---|
| Авторизация | Полный доступ к панели управления | Доступ к операционной панели компании | Доступ к модулю маршрутизации и мониторинга | Доступ к мобильному/адаптивному кабинету курьера | Вход в клиентский кабинет |
| Пользователи и роли | Управление сотрудниками, ролями и доступами | Просмотр состава команды | Просмотр назначенных курьеров | Просмотр собственного профиля | Редактирование персонального профиля |
| Работа с заказами | Полный контроль заказов | Создание и изменение заказов, ручные назначения | Планирование смен и маршрутов, перераспределение | Выполнение назначенных заказов, смена статусов доставки | Создание заказов, просмотр истории и трекинг |
| Логистика | Настройка справочников и правил | Контроль статусов и SLA | Управление хабами, сменами, маршрутами и картой | Передача геопозиции и подтверждение этапов | Просмотр статуса доставки |
| Коммуникации | Контроль уведомлений и чатов | Корпоративный чат с сотрудниками и клиентами | Корпоративный чат и уведомления по сменам | Личные/корпоративные чаты, уведомления | Чат по заказу, сервисные уведомления |
| Отчеты и биллинг | Доступ к аналитике и биллингу | Операционные отчеты компании | Логистическая аналитика | - | Просмотр стоимости и оплаты по заказу |

Администратор обладает расширенными правами и выполняет функции системного управления: контроль учетных записей, ролей, параметров компании, аудит и доступ к финансовому контуру. Менеджер работает в операционной части: создает и сопровождает заказы, взаимодействует с клиентами, контролирует статусы и качество исполнения.

Логист использует специализированные экраны планирования и мониторинга: формирует смены, управляет маршрутами, контролирует загрузку курьеров и оперативно реагирует на изменения в доставке. Курьер работает в адаптивном интерфейсе, где доступны сценарии начала/завершения смены, получение назначений, изменение статусов выполнения и коммуникация в чате.

Клиентский контур ориентирован на оформление заказов и контроль их исполнения: создание заказа, отслеживание этапов доставки, получение уведомлений, просмотр истории и базовое управление профилем.

Такое распределение прав и экранов обеспечивает информационную безопасность, снижает вероятность ошибочных действий и повышает эффективность выполнения бизнес-процессов в системе доставки.

**Рисунок 2.2 - Карта пользовательских интерфейсов DeliveryCRM**  
`[МЕСТО ДЛЯ РИСУНКА 2.2]`

Карта пользовательских интерфейсов приведена на рисунке 2.2 (см. рисунок 2.2). Ролевая декомпозиция интерфейсов обеспечивает быстрый доступ к целевым функциям и минимизирует пользовательские ошибки. Навигационная структура поддерживает последовательное выполнение рабочих сценариев и единообразие взаимодействия с системой.

### 2.5 Диаграмма вариантов использования (Диаграмма Use-case)

Диаграмма вариантов использования представляет собой модель, в которой фиксируются связи между акторами и функциями системы [14]. Формально такая диаграмма может рассматриваться как граф, где вершинами выступают акторы и прецеденты, а ребрами - отношения между ними [14]. Данный инструмент позволяет наглядно определить границы функциональности и распределение ответственности между участниками.

Под актором в работе понимается внешний по отношению к DeliveryCRM субъект, взаимодействующий с системой через web- или мобильный интерфейс [14]. Вариант использования (прецедент) описывает последовательность действий системы, приводящую к достижению значимого для актора результата [15].

На этапе построения use-case модели были выделены основные роли предметной области:
1. **Клиент** - пользователь клиентского кабинета. Выполняет авторизацию, создание заказа, заполнение параметров доставки, отслеживание статуса и работу с историей заказов, а также получает уведомления и использует чат сопровождения.
2. **Курьер** - сотрудник службы доставки, работающий в мобильном/адаптивном контуре. Выполняет авторизацию, начало и завершение смены, просмотр назначенных заказов, обновление этапов доставки и подтверждение выполнения заказа.
3. **Менеджер** - оператор компании, работающий в web-интерфейсе. Отвечает за обработку заказов, изменение статусов, коммуникацию с клиентами и контроль операционного потока.
4. **Логист** - сотрудник логистического контура. Формирует смены и маршруты, распределяет заказы между курьерами, контролирует карту, загрузку курьеров и показатели исполнения.
5. **Администратор** - пользователь с расширенными правами доступа. Управляет сотрудниками и ролями, системными настройками, аудитом и административными разделами платформы.

Для повышения наглядности сформированы отдельные диаграммы вариантов использования по ролям и итоговая объединяющая диаграмма.

**Рисунок 2.9 - Диаграмма вариантов использования для клиента**  
`[МЕСТО ДЛЯ РИСУНКА 2.9]`

**Рисунок 2.10 - Диаграмма вариантов использования для курьера**  
`[МЕСТО ДЛЯ РИСУНКА 2.10]`

**Рисунок 2.11 - Диаграмма вариантов использования для менеджера и логиста**  
`[МЕСТО ДЛЯ РИСУНКА 2.11]`

**Рисунок 2.12 - Диаграмма вариантов использования для администратора**  
`[МЕСТО ДЛЯ РИСУНКА 2.12]`

**Рисунок 2.13 - Общая диаграмма вариантов использования DeliveryCRM**  
`[МЕСТО ДЛЯ РИСУНКА 2.13]`

Построенные диаграммы демонстрируют иерархию прав доступа и поэтапное расширение функциональных возможностей от клиентского контура к административному. Каждая роль получает строго определенный набор операций, что обеспечивает защиту критичных функций и управляемость бизнес-процессов.

Таким образом, диаграммы вариантов использования дают целостное представление о функциональных требованиях DeliveryCRM, фиксируют границы системы и формализуют взаимодействие между категориями пользователей.

## 3 РЕАЛИЗАЦИЯ ПРОЕКТА

### 3.1 Обоснование выбора языка и средства разработки

Выбор технологического стека выполнен с учетом требований к производительности, расширяемости, информационной безопасности и удобству сопровождения.

Для реализации проекта выбраны:
- язык программирования `C#`;
- серверная платформа `ASP.NET Core`;
- web-интерфейс `Blazor Server`;
- система управления данными `PostgreSQL`;
- ORM-инструмент `Entity Framework Core`;
- подсистема realtime-взаимодействия `SignalR`.

Указанный стек обеспечивает единое технологическое пространство для серверной и клиентской разработки, а также сокращает затраты на поддержку и внедрение новых функций.

Результаты сравнения выбранного стека с альтернативами учитывали доступность специалистов, зрелость экосистемы, качество документации и практический опыт внедрения в похожих проектах. Сформированный набор технологий обеспечивает устойчивую эксплуатацию решения как на этапе пилотного внедрения, так и при дальнейшем расширении функционала.

Технологический стек соответствует требованиям к надежности, безопасности и масштабируемости системы. Выбранные инструменты обеспечивают эффективную поддержку, развитие проекта и интеграцию дополнительных модулей.

### 3.2 Создание проекта

Создание проекта выполнено по модульному принципу с выделением функциональных блоков и общей инфраструктуры доступа к данным.

В рамках реализации разработаны:
- модуль аутентификации и авторизации пользователей;
- модуль управления заказами и статусами;
- модуль логистического распределения и сопровождения доставки;
- модуль коммуникаций (чат и уведомления);
- модуль отчетности и аналитики;
- административный модуль с настройками и аудитом.

На этапе реализации также обеспечена согласованность API-контракта, ролевых сценариев интерфейса и структуры базы данных.

**Рисунок 3.1 - Структура проекта DeliveryCRM**  
`[МЕСТО ДЛЯ РИСУНКА 3.1]`

Структура проекта и взаимосвязь модулей показаны на рисунке 3.1 (см. рисунок 3.1). Реализованные модули формируют единый рабочий контур системы и обеспечивают сквозное выполнение ключевых бизнес-сценариев службы доставки. Интеграция компонентов подтверждает готовность решения к практической эксплуатации.

## 4 ТЕСТИРОВАНИЕ ПРОГРАММНОГО ПРОДУКТА

### 4.1 Основы тестирования программного продукта

Тестирование программного продукта направлено на подтверждение соответствия реализованного решения установленным требованиям. В рамках проверки контролируются функциональность, надежность взаимодействия модулей, корректность ролевого доступа и устойчивость ключевых пользовательских сценариев.

Основные направления тестирования:
- функциональное тестирование операций по заказам и ролям;
- интеграционное тестирование взаимодействия API, БД и клиентских экранов;
- проверка ограничений доступа и tenant-изоляции данных;
- проверка корректности отображения статусов и уведомлений.

**Таблица 4.1 - Матрица тестового покрытия**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 4.1]`

Матрица тестового покрытия представлена в таблице 4.1 (см. таблицу 4.1). Принятый подход к тестированию обеспечивает контроль качества на уровне критичных бизнес-функций. Тестовое покрытие снижает риск эксплуатационных дефектов и повышает предсказуемость поведения системы в рабочей среде.

### 4.2 Тест-кейс тестирование

На этапе test-case тестирования выполнена проверка сценариев, отражающих типовые действия пользователей в системе. Для каждого кейса зафиксированы входные условия, последовательность шагов, ожидаемый результат и фактический результат.

Проверены сценарии:
- авторизация и ролевой переход к рабочему интерфейсу;
- создание и обработка заказа;
- назначение курьера и изменение статусов доставки;
- обмен сообщениями и доставка уведомлений;
- формирование аналитических представлений и отчетов.

**Таблица 4.2 - Результаты тест-кейсов**  
`[МЕСТО ДЛЯ ТАБЛИЦЫ 4.2]`

Результаты тест-кейсов сведены в таблицу 4.2 (см. таблицу 4.2). Результаты test-case проверки подтверждают корректную работу ключевых сценариев. Критические ошибки, блокирующие эксплуатацию системы, не выявлены.

## 5 РУКОВОДСТВО ПОЛЬЗОВАТЕЛЯ

### 5.1 Руководство пользователя для клиента

Пользователь с ролью клиента выполняет вход в систему, создает заявку на доставку, заполняет параметры заказа и отслеживает изменение статусов исполнения. В процессе сопровождения заказа доступна коммуникация через встроенный чат и получение системных уведомлений.

**Рисунок 5.1 - Экран клиента**  
`[МЕСТО ДЛЯ РИСУНКА 5.1]`

Ключевые элементы клиентского интерфейса приведены на рисунке 5.1 (см. рисунок 5.1). Клиентский сценарий поддерживает полный цикл взаимодействия с системой от создания заказа до получения результата. Интерфейс обеспечивает доступность ключевых операций без избыточных действий.

### 5.2 Руководство пользователя для сотрудников кухни

Раздел адаптирован под операционный контур обработки заказов и обновления статусов исполнения. Пользователь данного контура работает со списком поступивших заявок, подтверждает этапы обработки и передает заказ в следующий этап логистического процесса.

**Рисунок 5.2 - Экран оператора обработки заказов**  
`[МЕСТО ДЛЯ РИСУНКА 5.2]`

Экран операционного контура приведен на рисунке 5.2 (см. рисунок 5.2). Операционный контур обеспечивает своевременную обработку и актуализацию статусов исполнения. Это повышает прозрачность процесса и сокращает задержки при передаче заказа между этапами.

### 5.3 Руководство пользователя для курьера

Пользователь с ролью курьера получает назначенные заказы, принимает их в работу, выполняет доставку по маршрутным точкам и фиксирует завершение каждого этапа. В интерфейсе доступны текущие задания, навигационный контур и история выполненных операций.

**Рисунок 5.3 - Экран курьера**  
`[МЕСТО ДЛЯ РИСУНКА 5.3]`

Курьерский интерфейс и основные команды представлены на рисунке 5.3 (см. рисунок 5.3). Курьерский интерфейс поддерживает последовательное выполнение доставки и контроль этапов исполнения. Обновление статусов в реальном времени улучшает диспетчерский контроль и информирование клиента.

### 5.4 Руководство пользователя для администратора

Пользователь с ролью администратора управляет учетными записями, ролевой моделью, системными справочниками и параметрами эксплуатации. Дополнительно осуществляется контроль журнала аудита для анализа действий пользователей и расследования инцидентов.

**Рисунок 5.4 - Экран администратора**  
`[МЕСТО ДЛЯ РИСУНКА 5.4]`

Основные элементы административной панели приведены на рисунке 5.4 (см. рисунок 5.4). Административный контур обеспечивает безопасное управление доступом и эксплуатационными настройками. Централизованный контроль повышает надежность работы системы и поддерживает регламентное администрирование.

## ЗАКЛЮЧЕНИЕ

Разработанная система `DeliveryCRM` обеспечивает единый цифровой контур управления службой доставки: от приема заказа до аналитики и административного контроля. Проектные и программные решения соответствуют цели и задачам, сформулированным во введении.

Проведенные этапы проектирования, реализации и тестирования подтверждают практическую применимость системы. Архитектура решения поддерживает дальнейшее развитие и масштабирование.

## СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ

1. Microsoft Learn. ASP.NET Core documentation. https://learn.microsoft.com/aspnet/core  
2. Salesforce CRM. https://www.salesforce.com  
3. Bitrix24. https://www.bitrix24.ru  
4. amoCRM. https://www.amocrm.ru  
5. Onfleet. https://onfleet.com  
6. Bringg. https://www.bringg.com  
7. NIST/ISO материалы по secure software development  
8. Kotler P., Keller K. Marketing Management  
9. PostgreSQL Documentation. https://www.postgresql.org/docs  
10. EF Core Documentation. https://learn.microsoft.com/ef/core  
11. SignalR Documentation. https://learn.microsoft.com/aspnet/core/signalr

## ПРИЛОЖЕНИЕ А

В приложении А приведены данные таблиц и полный текст SQL-скриптов создания (`CREATE`) и заполнения (`INSERT`) базы данных.

## ПРИЛОЖЕНИЕ Б

В приложении Б приведены листинги программного кода ключевых модулей проекта. Для оформления приложения Б допускается шрифт 10 и верстка в 2-3 колонки для сокращения объема страниц.

## ПРИЛОЖЕНИЕ В

Дополнительные материалы: расширенные формы отчетов, диаграммы и тестовые таблицы.
