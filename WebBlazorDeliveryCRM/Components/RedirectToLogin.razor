@inject NavigationManager Navigation

<p>Перенаправление на страницу входа… <a href="/">Войти</a></p>

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/", forceLoad: true);
    }
}
